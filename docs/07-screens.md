> 문서: 07-screens.md · 상태: 초안 · 열린 질문 [?] 27개 · 채택한 [제안] 8개 · 마지막 갱신: 금
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
> 종합 메모: `ShooT 하윤.dc.html`(디자인, 하윤 작업)에 실제로 그려진 화면 23개를 그대로 목록화하고, 03-requirements.md(R)·04-features.md(F)와 대조했다. 화면ID는 디자인 파일이 화면마다 붙여 둔 라벨("0.", "2b-1." 등)을 그대로 썼다. 디자인에 없는 화면(정산·리포트 등)은 만들어 넣지 않고 "문서에는 있으나 디자인엔 없음" 표에 모았다.

## 화면 목록
| 화면ID | 화면명 | 관련 R·F | 진입 경로 | 다음 화면(액션) |
|---|---|---|---|---|
| 0 | 스플래시(앱 실행) | — | 앱 실행 | 자동으로 1(비로그인) 또는 2b(로그인 유지 상태)로 — [?] 분기 조건이 디자인엔 없음 |
| 1 | 회원가입 — 입력 | (인증, R 없음 — SHY 스펙 Phase 1 필수) | 0 또는 2에서 "회원가입" 탭 | "가입하기" → 1b |
| 1b | 회원가입 — 성공 | 위와 동일 | 1에서 가입 성공 | "가입하기"(재확인 후) → [?] 2 또는 2b, 디자인엔 다음 동작이 없음 |
| 2 | 로그인 — 입력 | (인증) | 0 또는 1에서 "로그인" 탭 | "로그인" → 2a |
| 2a | 로그인 — 성공 | 위와 동일 | 2에서 로그인 성공 | → [?] 2b, 디자인엔 다음 동작이 없음 |
| 2b | 메인(홈) | R13·R17·R21 · F13·F21 | 로그인 후 진입, 하단 탭 "홈" | "그룹 만들기"→3a · 수입 카드→2b-1a · 하단 탭→5a/6/7/10 |
| 2b-1a | 수입 내역 | [?] 01~05에 없음(06-data.md E7 참고) | 2b 수입 카드 탭 | "+"→2b-1 |
| 2b-1 | 이번 달 수입 입력 | [?] 위와 동일 | 2b-1a "+" 버튼 | "추가하기" → [?] 2b-1a로 복귀 추정, 디자인엔 다음 동작 없음 |
| 2c | 설정(MVP) | 카테고리 관리 일부는 R16 관련, 알림·다크모드·글자크기는 [?] 01~05에 없음 | 10(마이페이지) 톱니바퀴 아이콘 | 각 토글·select 조작(화면 전환 없음) |
| 3a | 그룹 생성 — 모임 선택 | R1 · F1 | 2b "그룹 만들기" | 유형 카드 선택 후 "다음으로" → 3b-1~6 중 하나 |
| 3b-1~6 | 이름 입력(가족·부부·커플·룸메이트·모임동아리·기타) | R1 · F1 | 3a에서 유형 선택 | "그룹 만들기" → 3c |
| 3c | 그룹 생성 — 완료 | R1·R2 · F1·F2 | 3b-x에서 생성 완료 | "초대 코드 공유하기"(공유 시트, 화면전환 없음) · "홈으로 가기" → 2b |
| 4 | 그룹 참여 | R3 · F3 | [?] 진입 경로가 디자인에 없음 — 5a "초대 코드로 참여하기"에서 오는 것으로 추정 | "참여하기" → [?] 5a 추정, 디자인엔 다음 동작 없음 |
| 5a | 그룹 목록 | R1~R3 · F1~F3 | 하단 탭 "그룹" | 그룹 카드 탭 → 5b · "초대 코드로 참여하기" → 4 |
| 5b | 그룹 상세(피드) | R13 · F13, 저금 관련 부분은 [?](06-data.md E6 참고) | 5a에서 그룹 카드 탭 | 사람 아이콘 → 멤버 목록 패널 토글(같은 화면) |
| 6 | 지출 입력 | R5~R7, R12, R16 · F5~F7, F12, F16 | 하단 탭 중앙 "+"(추정, 아래 [?] 참고) | "저장하기" → [?] 7 추정, 디자인엔 다음 동작 없음 |
| 7 | 지출 목록 | R6, R13, R14 · F6, F10, F14 | 하단 탭 "지출내역" | 항목 탭 → [?] 수정 화면(디자인엔 별도 화면 없음, 6 재사용 추정) |
| 7b | 지출 목록 — 빈 상태 | 위와 동일(데이터 없을 때) | 지출이 하나도 없을 때 7 대신 표시 | "영수증으로 기록하기" → 8a |
| 8a | 영수증 촬영 | R8 · F8 | 7b 버튼, 또는 [?] 7에서의 진입 경로 없음 | 촬영/선택 → 8b |
| 8b | 영수증 인식 중 | R8, R10, R11 · F8, F11 | 8a 촬영 직후 | 인식 완료 → [?] 7(맨 위에 새 항목), 디자인엔 다음 동작 없음 |
| 10 | 마이페이지 | (그룹장 위임·나가기는 10a) | 하단 탭 "마이페이지" | 톱니바퀴 → 2c · "내 그룹 관리" → 10a · "로그아웃" → [?] 0 또는 2 추정 |
| 10a | 내 그룹 관리 | R18 · F18 | 10 "내 그룹 관리" | "그룹장 위임"·"그룹 나가기" 버튼(다음 화면 [?], 아래 참고) |

## 화면별 상세

### 0. 스플래시
- 표시: 로고, "우리가 정한 방식대로, 함께" 문구, 로딩 스피너
- [?] 로그인 여부에 따른 분기 로직이 디자인에 없다 — 로그인 상태 유지(2번 화면 "로그인 상태 유지" 토글, R 근거: SHY 스펙 Task 1-2 "onAuthStateChange") 시 스플래시 다음 화면이 2b인지, 매번 2인지 팀 확인 필요

### 1 / 1b. 회원가입
- 입력: 이메일, 비밀번호
- 예외: [?] 디자인엔 "이메일 형식 오류"·"비밀번호 8자 미만" 같은 유효성 검증 실패 화면이 없다(입력칸 placeholder "8자 이상"만 있음) — 03-requirements.md·05-policy.md에도 회원가입 유효성 규칙(P)이 없어 팀 확인 필요

### 2 / 2a. 로그인
- 입력: 이메일, 비밀번호, "로그인 상태 유지" 토글
- 예외: [?] 로그인 실패(비밀번호 틀림 등) 화면이 디자인에 없음

### 2b. 메인(홈)
- 표시: 그룹 선택 select(나 개인·민준&서연·행복한 우리집·전세 405호), 이번 달 총수입 카드, 이번 달 총지출 카드(카테고리별 비중 바), "그룹 만들기" 버튼(그룹이 없을 때만 노출되는 것으로 보임 — [?] 이미 그룹이 있어도 계속 보이는지), 최근 지출 3건, 9월 캘린더(날짜별 지출·수입 표시)
- 출처: R13(그룹 지출 한곳에서 보기), R17(카테고리 비중 — 04-features.md F17은 "월별 리포트"를 별도 화면·우선순위 "하(v2)"로 분류했는데, 이 요약 바는 홈(MVP 화면)에 이미 들어있어 **[제안] F17의 일부가 이미 MVP 홈 화면에 구현돼 있다고 봐야 하는지 팀 확인 필요** — 근거: 04-features.md "F17 하/v2" 표기와 이 홈 화면이 서로 다른 범위를 말하고 있음), R21(캘린더)
- **[?] 캘린더에 저금(Saving)이 표시되지 않는다** — 디자인 `CALENDAR_DATA`는 `expense`/`income` 키만 있고 저금 데이터가 없다. 03-requirements.md R21은 "지출 금액과 저금 금액을 함께" 보라고 정했는데, 이 화면은 지출·수입만 보여준다(저금 아님) — 06-data.md의 Income 관련 [?]와 함께 팀 확인 필요

### 2b-1a / 2b-1. 수입 내역 · 입력
- 06-data.md E7 참고 — 01~05에 없는 기능이라 이 두 화면 전체가 [?]

### 2c. 설정(MVP)
- 표시: 카테고리 목록(수정 항목 자체는 UI만 있고 편집 액션 없음), "새 카테고리 추가"(다이얼로그 등 다음 화면 [?]), 알림 토글 3종(지출 기록 확인·예산 초과·알림음), 다크모드 토글, 글자 크기 select
- **[?] 01~05 어디에도 없는 설정 항목들** — "예산 초과 시 알림"은 01-problem.md·03~05에 "예산" 개념 자체가 없고, 다크모드·글자크기·알림음은 순수 UI 설정이라 R/F/P로 다뤄진 적이 없음. 카테고리 관리(추가·수정)는 R16(그룹 유형별 프리셋)과 관련은 있으나, "사용자가 설정 화면에서 카테고리를 직접 추가·수정"하는 것까지는 R16 문장에 없음 — 팀이 이번 범위에 포함할지 확인 필요

### 3a. 그룹 생성 — 모임 선택
- 표시: 유형 카드 6개(가족·부부·커플·룸메이트·모임동아리·기타)
- **[?] "형제자매" 카드 없음** — 06-data.md E2 항목 참고(SHY 스펙 enum엔 SIBLING이 있으나 이 화면엔 카드가 없음)

### 3b-1~6. 이름 입력
- 입력: 그룹 이름(자유 텍스트), 추천 이름 칩 3개(유형별로 다름)
- 출처: R1 · SHY 스펙 Task 1-4("플레이스홀더는 선택한 유형에 따라 예시를 다르게")

### 3c. 그룹 생성 — 완료
- 표시: 완료 아이콘, 그룹명, 초대 코드(예: CP-4K9X), "초대 코드 공유하기" 버튼
- 출처: R2 · F2 · SHY 스펙 Task 1-4

### 4. 그룹 참여
- 입력: 초대 코드
- 예외: [?] "코드 없음"·"이미 참여한 그룹"(P3, 409 오류) 화면이 디자인엔 없음 — 04-features.md F3 예외("이미 참여한 그룹입니다" 오류)와 대조 필요

### 5a. 그룹 목록
- 표시: 그룹 카드(그룹명, 유형 배지, 멤버 수), "초대 코드로 참여하기" 진입 카드
- 출처: R1~R3

### 5b. 그룹 상세(피드)
- 표시: 그룹명·유형, (패널 토글 시) 멤버 목록 + 초대 코드 공유, 지출·저금 피드(작성자 아바타, 가맹점명, 카테고리, 시간, 금액 — 수입/지출 색상 구분)
- 출처: R13 · F13
- **[?] "지출·저금 피드"라는 제목과 달리 실제 데이터(`FEED_ITEMS_BASE`)엔 저금 항목이 "9월 저금"이라는 지출류 카드 하나로만 섞여 있다** — 06-data.md E6(Saving)과 이 피드의 관계(같은 목록에 섞는지, 별도 섹션인지)가 정의돼 있지 않음

### 6. 지출 입력
- 표시: 상단 금액(디자인엔 고정 텍스트 "12,500"로만 있고 실제 숫자 입력 UI가 안 보임 — [?] 금액 입력 방식 확인 필요), 카테고리(그룹 유형별 프리셋, 06-data.md E8 참고 + "직접 입력"), 메모, 날짜, "어느 그룹과 공유할까요?" select
- 출처: R5~R7, R12, R16 · F5~F7, F12, F16
- **[제안] 이 화면은 그룹을 먼저 고르면(select) 카테고리 프리셋이 바뀌는 순서다.** SHY 스펙 Task 2-4도 "그룹 선택 → 프리셋 전환"으로 정해서 이 디자인과 일치함 — 근거: SHY 스펙 Task 2-4 설명

### 7 / 7b. 지출 목록
- 표시: 월별 그룹핑된 지출 카드(가맹점명, "확인 필요" 배지, 카테고리·그룹·시간, 금액), 데이터 없을 때(7b) 빈 상태 안내
- 출처: R6, R10, R13, R14 · F6, F10, F14
- **[?] 필터 UI가 없다** — 04-features.md F6 입력에 "category, startDate, endDate(필터)"가 있는데, 이 화면엔 월별 묶음만 있고 필터·검색 UI가 없음

### 8a / 8b. 영수증 촬영·인식 중
- 출처: R8, R10, R11 · F8, F11 · SHY 스펙 Task 3-3
- 인식 완료 시 하단에 "인식 완료! ₩12,500 · 식비 · 스타벅스" 카드가 뜸(토스트 형태) — F8 결과("OO원, XX마트 지출이 자동으로 등록됐어요" 토스트)와 문구는 다르지만 취지는 일치

### 10 / 10a. 마이페이지 · 내 그룹 관리
- 표시(10): 프로필(이름·이메일), "내 그룹 관리", 로그아웃
- 표시(10a): 소속 그룹별 카드 — 그룹장인 그룹은 "그룹장 위임"·"그룹 나가기" 버튼, 일반 멤버인 그룹은 "그룹 나가기" 버튼만
- 출처: R18 · F18 · 05-policy.md P10, 상태값1
- **[?] "그룹장 위임" 버튼을 누른 다음 화면이 디자인에 없다** — 04-features.md F18 트리거 자체가 "[?] 트리거(화면 진입 방식)만 아직 [제안]"이라고 04에 이미 표시돼 있음. 위임 대상(그룹원 중 1명)을 고르는 화면·목록 UI가 필요하나 이 디자인엔 없음
- **[?] "그룹 나가기" 확인 절차가 없다** — 그룹원이 혼자뿐이면 그룹·지출이 함께 삭제되는(05-policy.md 상태값1) 되돌릴 수 없는 동작인데, 확인 다이얼로그 화면이 디자인엔 없음

## 하단 탭 내비게이션
5개 탭(홈 · 그룹 · 중앙 강조 아이콘 · 지출내역 · 마이페이지)이 2b·5a·5b·6·7·7b·10 화면에 공통으로 나타난다.
- 홈 → 2b, 그룹 → 5a, 지출내역 → 7, 마이페이지 → 10
- **[?] 중앙 강조 아이콘(카메라 모양)이 어디로 연결되는지 정해져 있지 않다** — 아이콘 모양은 8a(영수증 촬영)의 카메라 버튼과 같은 SVG를 쓰지만, 6번 화면(지출 입력) 진입도 이 탭을 통해서인지, 아니면 6과 8a가 별도 진입점을 갖는지 디자인에 명시가 없음

## 화면 전환 방식
> 2026-09-11 팀 결정: 화면마다 라우트를 만들지 않는다. `app/page.tsx` 한 페이지가 화면 23개를 상태로 들고 전환한다. 위 「화면 목록」 표의 진입 경로·다음 화면(액션)은 그대로다 — 이동이 일어나는 방식만 페이지 이동에서 화면 전환(애니메이션)으로 바뀐다.
> 한 페이지로 합친 이유: 라우트를 화면마다 두면 페이지 이동마다 이전 화면이 언마운트되는데, 아래 "화면 쌓기"는 여러 겹이 동시에 마운트된 채 하나씩 벗겨지는 세로 슬라이드를 요구하고 "탭 전환"은 120ms 크로스페이드를 요구한다 — 둘 다 이전 화면이 전환 도중 화면에 남아 있어야 해서, 라우트 이동이 아니라 한 페이지 안의 상태 전환으로 구현한다.

이동은 두 종류이고 애니메이션이 다르다.

| 종류 | 대상 | 애니메이션 | 뒤로가기 |
|---|---|---|---|
| (1) 탭 전환 | 「하단 탭 내비게이션」의 홈(2b)·그룹(5a)·지출내역(7)·마이페이지(10) 사이 | 슬라이드 없음, 120ms 크로스페이드 | 개념 없음(탭끼리는 뒤로가기가 없음) |
| (2) 화면 쌓기 | 탭 위에 얹히는 화면 | 세로 슬라이드 280ms · cubic-bezier(0.32, 0.72, 0, 1) — 앞으로 가면 아래에서 올라오고, 뒤로 가면 아래로 내려간다. 여러 겹 쌓이고 하나씩 벗겨진다. 쌓인 화면에는 하단 탭이 보이지 않는다 | 한 겹씩 벗겨짐 |

### 화면ID별 분류
팀이 예로 든 것은 2b·5a·7·10(탭 전환)과 3a→3b-x→3c·4·5b·6(화면 쌓기)뿐이다. 나머지 화면ID 중 화면 목록의 진입 경로로 볼 때 같은 패턴(탭 위에 얹힘)이 명확한 것은 [제안]으로 표시해 채웠고, 그렇지 않은 것(로그인 전 화면, 7b)은 팀이 정하지 않은 것으로 남긴다.

| 화면ID | 방식 | 근거 |
|---|---|---|
| 0 | [?] | 로그인 전 진입점이라 아직 탭 셸 자체가 없다 — "탭 전환"·"화면 쌓기" 정의가 이 화면에도 적용되는지 이번 결정 범위 밖 |
| 1 | [?] | 로그인 전, 탭 셸이 없어 위와 동일 |
| 1b | [?] | 위와 동일 |
| 2 | [?] | 위와 동일 |
| 2a | [?] | 위와 동일 |
| 2b | (1) 탭 전환 | 팀 결정에 명시 |
| 2b-1a | [제안] (2) 화면 쌓기 | 화면 목록의 진입 경로("2b 수입 카드 탭")가 홈 탭(2b) 위에 얹히는 형태와 일치 |
| 2b-1 | [제안] (2) 화면 쌓기 | 진입 경로("2b-1a '+' 버튼")가 2b-1a 위에 다시 얹히는 형태 — "여러 겹 쌓이고 하나씩 벗겨진다"는 정의와 일치 |
| 2c | [제안] (2) 화면 쌓기 | 진입 경로("10 톱니바퀴 아이콘")가 마이페이지 탭(10) 위에 얹히는 형태와 일치 |
| 3a | (2) 화면 쌓기 | 팀 결정에 명시 |
| 3b-1~6 | (2) 화면 쌓기 | 팀 결정에 명시 |
| 3c | (2) 화면 쌓기 | 팀 결정에 명시 |
| 4 | (2) 화면 쌓기 | 팀 결정에 명시 |
| 5a | (1) 탭 전환 | 팀 결정에 명시 |
| 5b | (2) 화면 쌓기 | 팀 결정에 명시 |
| 6 | (2) 화면 쌓기 | 팀 결정에 명시 |
| 7 | (1) 탭 전환 | 팀 결정에 명시 |
| 7b | 해당 없음(전환 아님) | 화면 목록에 "지출이 하나도 없을 때 7 대신 표시"라고 돼 있어, 7과 같은 탭 자리의 데이터 없음 상태일 뿐 별도 전환이 일어나지 않음 |
| 8a | [제안] (2) 화면 쌓기 | 진입 경로("7b 버튼")가 지출내역 탭(7) 위에 얹히는 형태와 일치 |
| 8b | [제안] (2) 화면 쌓기 | 8a 위에 이어서 얹히는 형태 |
| 10 | (1) 탭 전환 | 팀 결정에 명시 |
| 10a | [제안] (2) 화면 쌓기 | 진입 경로("10 '내 그룹 관리'")가 마이페이지 탭(10) 위에 얹히는 형태와 일치 |

**[?] 0·1·1b·2·2a(스플래시·회원가입·로그인)의 전환 방식은 이번 결정에 없다.** 로그인 전이라 탭 셸 자체가 아직 없어 "탭 전환"도 "화면 쌓기"도 그대로 들어맞는지 불명확하다 — 지어내지 않고 팀 확인으로 남긴다.

## 문서(01~05)에는 있으나 디자인엔 없는 화면·기능
| 후보 | 근거(문서) | 비고 |
|---|---|---|
| 연락처로 초대(F4) | 04-features.md F4 | 그룹 초대 화면(3c)엔 초대 코드 공유만 있고 연락처 선택 UI 없음 — 04에도 이미 "우선순위 하·v2"로 분류돼 있어 이번 범위엔 없을 수 있음 |
| 간편결제 캡쳐 등록(F9) | 04-features.md F9 · SHY 스펙 Task 4-1·4-2 | 영수증 촬영(8a·8b)만 있고 카카오페이·토스·네이버페이 캡쳐 업로드 화면 없음 — 04에도 "우선순위 하·v2" |
| 정산(더치페이) 화면(F15) | 04-features.md F15 · 03-requirements.md R15 | "정산하기" 버튼·계산 결과 화면이 디자인 전체에 없음(화면 번호 "9."가 아예 없음) |
| 월별 리포트 화면(F17) | 04-features.md F17 · SHY 스펙 Task 6-1·6-2 | 도넛차트·카테고리 비중 화면 없음(홈의 막대 비중 요약과는 별개 — 위 "2b" 상세 참고) |
| 저금 추가 입력 화면(F19·F20) | 04-features.md F19·F20 · 03-requirements.md R19·R20 | 06-data.md E6 참고 — 그룹 피드의 카드 하나로만 등장, 별도 입력 폼 없음 |
| 지출 수정 폼(F14) | 04-features.md F14 | 목록(7)에서 항목 탭 후 나오는 화면이 디자인에 없음(입력 폼 6번을 재사용한다고 추정만 가능) |

## 검토했으나 제외
| 후보 | 출처 | 제외 이유 |
|---|---|---|
| 수입(Income) 화면(2b-1·2b-1a)을 이 문서의 정식 화면 목록에서 빼는 것 | 종합 시 검토 | 디자인에 실제로 존재하는 화면이라 빼지 않고 목록엔 넣되, 01~05 근거가 없다는 점을 [?]로 표시하는 쪽을 택함 |
