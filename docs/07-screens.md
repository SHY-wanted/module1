> 문서: 07-screens.md · 상태: 초안(디자인 23개 화면은 대부분 확정, 아래 저금통 펫 절은 미구현 제안) · 열린 질문 [?] 7개(전부 저금통 펫 절) · 채택한 [제안] 2개 · 마지막 갱신: 화
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
> 종합 메모: `ShooT 하윤.dc.html`(디자인, 하윤 작업)에 실제로 그려진 화면 23개를 그대로 목록화하고, 03-requirements.md(R)·04-features.md(F)와 대조했다. 화면ID는 디자인 파일이 화면마다 붙여 둔 라벨("0.", "2b-1." 등)을 그대로 썼다. 디자인에 없는 화면(정산·리포트 등)은 만들어 넣지 않고 "문서에는 있으나 디자인엔 없음" 표에 모았다. **2026-09-11 팀 인터뷰로 이 문서에 남아있던 [?] 27개(「화면 전환 방식」 절 포함)를 전부 확인해 확정했다** — 아래 각 항목에 "2026-09-11 팀 결정"으로 표시해 뒀다. 이 과정에서 10a-1(위임 대상 선택) 화면이 새로 추가됐고, 수입(2b-1a·2b-1)·설정(2c)이 이번 범위에 포함됐다.
> **2026-09-15 추가**: docs/08-pet-feature-spec.md(저금통 펫 키우기 — 신규 제안)를 「저금통 펫 키우기」 절로 병합했다. 이 절은 `ShooT 하윤.dc.html`(23개 화면)과 무관한 별도 프로토타입이라 위 화면 목록·화면ID별 분류에는 포함하지 않았다. 사용자 요청으로 기획 문서 반영까지만 진행 — 코드 구현은 아직 없다.
> **2026-09-15 갱신(종민 확인)**: 아래 「구현 전 확인 필요 [?] 목록」 7개 중 3개(항목 2·3·6)에 종민이 방향을 정했다 — 세부는 docs/08-pet-feature-spec.md §9 참고. 나머지(1·4·5·7)와 새로 생긴 질문(그룹 펫 XP 획득 방식)은 여전히 [?]다. 이 갱신도 「기획 문서 반영까지만」 범위이며 코드 구현은 없다.

## 화면 목록
| 화면ID | 화면명 | 관련 R·F | 진입 경로 | 다음 화면(액션) |
|---|---|---|---|---|
| 0 | 스플래시(앱 실행) | — | 앱 실행 | 로그인 세션이 있으면(로그인 유지) → 2b(홈)로 자동(대기 없음), 없으면 화면에 머물다 "시작하기" 버튼을 눌러야 → 1(회원가입) — 2026-09-11 자동 분기 결정, 2026-09-14 팀 결정으로 "없으면" 쪽을 자동 타이머 대신 버튼 탭으로 변경 |
| 1 | 회원가입 — 입력 | (인증, R 없음 — SHY 스펙 Phase 1 필수) | 0 또는 2에서 "회원가입" 탭 | "가입하기" → 1b (형식 오류·8자 미만 비밀번호는 입력칸 밑 인라인 오류로 막음 — 2026-09-11 팀 결정) |
| 1b | 회원가입 — 성공 | 위와 동일 | 1에서 가입 성공 | "성공했습니다" 토스트를 1.3초 보여준 뒤 자동으로 → 2(로그인 입력) — **2026-09-17 팀 결정으로 변경**(기존 2026-09-11 결정 "2b로 자동 로그인"을 대체 — 재입력·재클릭 없이 자동 전환) |
| 2 | 로그인 — 입력 | (인증) | 0 또는 1에서 "로그인" 탭 | "로그인" → 2a (이메일/비밀번호 불일치는 입력칸 밑 인라인 오류 — 2026-09-11 팀 결정) |
| 2a | 로그인 — 성공 | 위와 동일 | 2에서 로그인 성공 | "성공했습니다" 토스트를 1.3초 보여준 뒤 자동으로 → 2b(홈) — 목적지는 2026-09-11 결정 그대로, **2026-09-17 팀 결정으로 재입력·재클릭 없이 자동 전환되도록 변경** |
| passwordResetInput | 비밀번호를 잊으셨나요(신규) | 01~05엔 없던 기능 | 2(로그인) "비밀번호를 잊으셨나요?" | 이메일 입력 → 재설정 링크 발송 → 링크는 `app/reset-password`(SPA 밖 별도 라우트)로 연결 — 2026-09-22 팀원 요청으로 신규 |
| 2b | 메인(홈) | R13·R17·R21 · F13·F21 | 로그인 후 진입, 하단 탭 "홈" | "그룹 만들기"→3a · 수입 카드→2b-1a · **총지출 카드 ">"→7(신규, 2026-09-11 팀 결정)** · 하단 탭→5a/7/10 |
| 2b-1a | 수입 내역 | 01~05엔 없던 기능 — 2026-09-11 팀 결정으로 이번 범위에 포함(신규, R/F 미부여) | 2b 수입 카드 탭 | "+"→2b-1 |
| 2b-1 | 이번 달 수입 입력 | 위와 동일 | 2b-1a "+" 버튼 | "추가하기" → 2b-1a로 복귀(다른 입력 폼과 같은 패턴) — 2026-09-11 팀 결정 |
| 2c | 설정(MVP) | 카테고리 관리 일부는 R16 관련, 알림·다크모드·글자크기는 01~05엔 없던 기능 — 2026-09-11 팀 결정으로 전부 이번 범위에 포함 | 10(마이페이지) 톱니바퀴 아이콘 | 각 토글·select 조작, 카테고리 추가·수정 가능(화면 전환 없음). "정기 지출 관리" 행 → recurringExpenseManage(신규, 2026-09-21) |
| recurringExpenseManage | 정기 지출 관리(신규) | 01~05엔 없던 기능 | 2c "정기 지출 관리" | 등록된 정기 지출 목록·활성 토글·삭제(화면 전환 없음) — 2026-09-21 사용자 요청 |
| 3a | 그룹 생성 — 모임 선택 | R1 · F1 | 2b "그룹 만들기" | 유형 카드 선택 후 "다음으로" → 3b-1~6 중 하나 |
| 3b-1~6 | 이름 입력(가족·부부·커플·친구·모임동아리·기타) | R1 · F1 | 3a에서 유형 선택 | "그룹 만들기" → 3c |
| 3c | 그룹 생성 — 완료 | R1·R2 · F1·F2 | 3b-x에서 생성 완료 | "초대 코드 공유하기"(공유 시트, 화면전환 없음) · "홈으로 가기" → 2b |
| 4 | 그룹 참여 | R3 · F3 | 5a "초대 코드로 참여하기"에서만 — 2026-09-11 팀 결정 | "참여하기"(성공) → 5a로 복귀. 실패(코드 없음·이미 참여한 그룹 P3)는 입력칸 밑 인라인 오류, 화면 전환 없음 — 2026-09-11 팀 결정 |
| 5a | 그룹 목록 | R1~R3 · F1~F3 | 하단 탭 "그룹" | 그룹 카드 탭 → 5b · "초대 코드로 참여하기" → 4 |
| 5b | 그룹 상세(피드) | R13 · F13, 저금(Saving)도 같은 피드에 시간순으로 섞어 표시 — 2026-09-11 팀 결정 | 5a에서 그룹 카드 탭 | 사람 아이콘 → 멤버 목록 패널 토글(같은 화면). "이번 달 그룹 예산" 카드 → groupGoalSetting(신규, 2026-09-21) |
| groupGoalSetting | 그룹 예산 설정(신규) | 01~05엔 없던 기능 | 5b 그룹 예산 카드 | 그룹장만 카테고리별 목표 금액 입력·저장 가능, 그룹원은 진행률만 확인(읽기 전용) — 2026-09-21 사용자 요청 |
| 6 | 지출 입력 | R5~R7, R12, R16 · F5~F7, F12, F16 | 7(지출 목록) 우상단 새 "+" 버튼(신규) — 2026-09-11 팀 결정(하단 탭 중앙 아이콘은 8a로 감, 아래 참고) | "저장하기" → 7(지출 목록)로 복귀 — 2026-09-11 팀 결정 |
| 7 | 지출 목록 | R6, R13, R14 · F6, F10, F14 | 하단 탭 "지출내역" | 항목 탭 → 6을 수정 모드로 재사용 · 우상단 "+"(신규) → 6(신규 입력) — 2026-09-11 팀 결정 |
| 7b | 지출 목록 — 빈 상태 | 위와 동일(데이터 없을 때) | 지출이 하나도 없을 때 7 대신 표시 | "영수증으로 기록하기" → 8a |
| 8a | 영수증 촬영 | R8 · F8 | 하단 탭 중앙 카메라 아이콘, 그리고 7b의 "영수증으로 기록하기" 버튼(진입점 2개) — 2026-09-11 팀 결정 | 촬영/선택 → 8b |
| 8b | 영수증 인식 중 | R8, R10, R11 · F8, F11 | 8a 촬영 직후 | 인식 완료 → 7(지출 목록, 맨 위에 새 항목) — 2026-09-11 팀 결정 |
| 10 | 마이페이지 | (그룹장 위임·나가기는 10a) | 하단 탭 "마이페이지" | 톱니바퀴 → 2c · "내 그룹 관리" → 10a · "로그아웃" → 2(로그인) — 2026-09-11 팀 결정 |
| 10a | 내 그룹 관리 | R18 · F18 | 10 "내 그룹 관리" | "그룹장 위임" → 10a-1(신규) · "그룹 나가기" → 확인 팝업(모달) 후 실행 — 2026-09-11 팀 결정 |
| 10a-1 | 위임 대상 선택(신규) | R18 · F18 | 10a "그룹장 위임" | 그룹원 목록에서 한 명 선택 → 위임 실행, 10a로 복귀 — 2026-09-11 팀 결정 |
| 10b | 내 정보 변경(신규) | R 없음(01~05에 없던 요청) | 10 프로필 카드 | "저장하기" → 10으로 복귀 — 2026-09-17 팀 요청 |
| personalRanking | 개인 랭킹(신규) | R 없음(01~05에 없던 요청) | 10 "개인 랭킹" 카드 | 개인 펫끼리(그룹과 무관) 성장 지표 기준 순위 표시(화면 전환 없음) — 2026-09-17 사용자 요청, 코드는 이미 있었으나 이 표에 누락돼 있었음 |

## 화면별 상세

### 0. 스플래시
- 표시: 로고, "우리가 정한 방식대로, 함께" 문구, **"시작하기" 버튼**("함께 쓰고, 함께 확인해요" 문구와 간격을 두고 그 자리에 위치 — 2026-09-14 팀 결정으로 로딩 스피너 자리를 대체함)
- 로그인 여부 분기(2026-09-11 팀 결정, 2026-09-14 일부 변경): 로그인 세션이 있으면(2번 화면 "로그인 상태 유지" 토글이 켜진 채 재실행) → 대기 없이 바로 2b(홈)로. 세션이 없으면(처음 쓰는 사람·로그아웃 상태) → 화면에 그대로 머물고, 더 이상 1~2초 후 자동으로 넘어가지 않는다 — 사용자가 "시작하기" 버튼을 눌러야 1(회원가입)로 이동한다(2026-09-14 팀 결정, 로딩 기능 제거).

### 1 / 1b. 회원가입
- 입력: **이름**(신규, 2026-09-14 팀 결정 — 디자인엔 없던 칸), 이메일, 비밀번호
- 이름 반영(2026-09-14 팀 결정): 1(회원가입 — 입력)에서 받은 이름이 2b(홈)의 "OO님 안녕하세요"·10(마이페이지) 프로필에 그대로 표시된다.
- 유효성 검증(2026-09-11 팀 결정): 이메일 형식 오류·비밀번호 8자 미만은 화면 전환 없이 입력칸 밑에 빨간 인라인 오류 문구로 표시. **2026-09-14 실제 구현: 이메일 형식 검증만 우선 적용됨**(1의 "가입하기"를 막고 "이메일 형식이 올바르지 않아요" 표시) — 비밀번호 8자 미만 검증은 아직 구현 안 됨(TODO로 남아있음).
- 1b 화면(2026-09-17 팀 결정): 이메일·비밀번호를 다시 입력하고 "가입하기"를 한 번 더 눌러야 다음으로 넘어가던 중복 제출 버그를 없앴다 — 이제 폼을 다시 보여주지 않고 "회원가입 성공했습니다!" 토스트만 1.3초 보여준 뒤 자동으로 2(로그인 입력)로 넘어간다.

### 2 / 2a. 로그인
- 입력: 이메일, 비밀번호, "로그인 상태 유지" 토글
- 실패 표시(2026-09-11 팀 결정): 이메일/비밀번호 불일치 시 화면 전환 없이 인라인 오류(예: "이메일 또는 비밀번호가 맞지 않아요"). **2026-09-17 실제 구현**: 더 이상 항상 성공 처리하지 않는다 — 1(회원가입)에서 저장한 이메일·비밀번호와 정확히 같을 때만 통과한다. **2026-09-18 실제 구현**: 목업 비교 대신 진짜 `supabase.auth.signInWithPassword`/`signUp`으로 바뀌었다(`store.signIn`/`store.signUp`/`store.signOut`, `lib/supabase/client.ts`) — 이메일 인증(Confirm email)이 켜진 프로젝트라면 가입 직후 바로 로그인이 안 될 수 있다.
- 2a 화면(2026-09-17 팀 결정): 1b와 같은 이유로 중복 제출 버그를 없앴다 — 폼을 다시 보여주지 않고 "로그인 성공했습니다!" 토스트만 1.3초 보여준 뒤 자동으로 2b(홈)로 들어간다.
- **카카오 로그인(2026-09-18 팀원 요청, 신규)**: 1(회원가입)·2(로그인) 둘 다에 "카카오로 계속하기" 버튼이 추가됐다(`KakaoLoginButton.tsx`) — 카카오 OAuth로 즉시 가입·로그인된다.
- **비밀번호를 잊으셨나요(2026-09-22 팀원 요청, 신규)**: 2(로그인)에 링크가 추가됐다 → passwordResetInput(위 화면 목록 참고) → 이메일 재설정 링크 발송.

### 2b. 메인(홈)
- 표시: 그룹 선택 select(나 개인·민준&서연·행복한 우리집·전세 405호), 이번 달 총수입 카드(">"→2b-1a), **이번 달 총지출 카드(">"→7, 신규 — 2026-09-11 팀 결정, 수입 카드와 같은 패턴)**, 카테고리별 비중 바, "그룹 만들기" 버튼(그룹이 있어도 항상 보임 — 2026-09-11 팀 결정), **최근 지출 5건(2026-09-17 팀 결정으로 3건→5건, "전체보기" 링크는 삭제 — store.expenses 기준으로 실시간 반영됨)**, 캘린더(날짜별 지출·수입만 표시, 저금은 이번엔 제외 — 2026-09-11 팀 결정)
- 캘린더 월 이동(2026-09-14 팀 결정): 9월 한 달 고정이 아니라 **1~12월 전부** 보여줄 수 있다. 캘린더 제목 옆 ‹ › 화살표로 달을 넘기면 연도도 자연스럽게 같이 바뀐다. 과거는 제한 없이 넘길 수 있고, 미래는 오늘(2026-09-09) 기준 **+1년(2027-09)까지만** — 그 이상은 › 화살표가 비활성화된다. "이번 달 총 수입·총 지출" 카드는 캘린더를 다른 달로 넘겨도 바뀌지 않고 항상 실제 오늘 기준 이번 달을 보여준다(별개 상태).
- 출처: R13(그룹 지출 한곳에서 보기), R17(카테고리 비중 — 04-features.md F17은 "월별 리포트"를 별도 화면·우선순위 "하(v2)"로 분류했는데, 이 요약 바는 홈(MVP 화면)에 이미 들어있어 **[제안] F17의 일부가 이미 MVP 홈 화면에 구현돼 있다고 봐야 하는지 팀 확인 필요** — 근거: 04-features.md "F17 하/v2" 표기와 이 홈 화면이 서로 다른 범위를 말하고 있음), R21(캘린더)
- **카테고리 비중 도넛 차트(2026-09-21 팀원 요청, 신규)**: 기존 막대형 비중 표시에 더해 도넛 차트(`CategoryDonut`)가 추가됐다.
- **온보딩 투어(2026-09-21 팀원 요청, 신규)**: `profiles.onboarding_seen`이 false인 계정이 처음 로그인하면(2b 진입 시) 영수증 촬영·그룹 공유·목표/펫 성장을 소개하는 슬라이드 3장(`OnboardingTour.tsx`)이 뜬다. 중간에 벗어나도 마지막 슬라이드의 "시작하기"를 눌러야만 완료 처리되어, 그 전엔 다음 로그인 때 다시 뜬다.

### 2b-1a / 2b-1. 수입 내역 · 입력 (2026-09-11 팀 결정으로 이번 범위에 포함)
- 06-data.md E7 참고 — 01~05엔 근거가 없던 기능이지만, 팀이 오늘 이번 범위에 포함하기로 정했다. 03~05에 R/F 번호는 아직 없다(필요하면 03-requirements.md·04-features.md에 별도로 추가해야 함 — 이 문서 범위 밖).
- 표시(2b-1a): "2026년 9월 총 수입" 요약 카드, 수입 항목 목록(제목·카테고리·날짜·금액)
- 입력(2b-1): 금액(직접 타이핑 + 1만/10만/100만 빠른 증가 버튼·초기화), 수입 항목(select: 급여·용돈·부수입·기타), 메모, "매달 자동으로 반복" 토글
- 삭제(2026-09-19 팀 요청): 각 항목에 삭제 버튼 추가 — 바로 지우지 않고 확인 팝업을 한 번 띄운다(10a "그룹 나가기"와 같은 패턴). ~~수입은 schema.sql에 테이블이 없어 로컬 상태만 지운다~~ **2026-09-18 해결**: `011_incomes_avatar_account_deletion.sql`로 실제 `incomes` 테이블·RLS가 생겨, 이제 실제 DELETE로 지운다(`store.deleteIncome`).

### 2c. 설정(MVP) (2026-09-11 팀 결정으로 전부 포함, 2026-09-17 대폭 변경)
- 카테고리(2026-09-17 팀 결정, 두 차례 갱신): "카테고리" 제목 옆 3줄 버튼 → "카테고리 편집" 메뉴 → 각 칸 "..."로 이름 변경. **"카테고리 추가" 버튼은 평소엔 없다가 "카테고리 편집" 모드에서만 나타난다**(맨 처음엔 완전히 없앴다가, 편집 모드 한정으로 다시 허용하는 것으로 변경). 각 칸의 아이콘은 전부 없앴다 — 색이 칠해진 원 하나로만 구별한다.
- **개인·그룹 카테고리 차별화(2026-09-17 팀 결정)**: 카테고리 목록 위에 "개인 카테고리 / OO 카테고리 / ..." select가 새로 생겼다 — 6(지출 입력)의 "어느 그룹과 공유할까요?"와 짝을 맞춰, 그룹마다 서로 다른 카테고리 목록을 따로 관리한다(그룹을 하나 고르면 그 그룹만의 목록이 뜨고, 편집·추가도 그 그룹에만 적용됨). 6에서 "직접 입력"으로 새 카테고리를 저장하면 그때 고른 scope(개인 또는 그 그룹)에 자동으로 추가되어 여기 목록에도 나타난다.
- 알림(2026-09-17 팀 결정): "예산 초과 시 알림" 행은 아예 삭제했다(예산 기능 자체가 없어서). **참고(2026-09-15 종민 확인, 위 「저금통 펫 키우기」 절 P7 참고)**: 저금통 펫 프로토타입 쪽에서 별도로 "예산" 개념(주간 예산 설정 화면)이 새로 제안됐다 — 아직 프로토타입 단계이고 이 실제 화면엔 반영 안 됨. 이 알림 행을 되살릴지는 팀이 따로 정해야 함. "알림음"은 **"그룹원 기록 확인 알림"**으로 대체했다 — 내가 속한 그룹에 (나 말고) 다른 그룹원이 지출을 기록하면 알려주는 취지이며, 예시 문구는 "민준 & 서연 · 민준님이 기념일 비용을 저장하였어요"처럼 그룹명·닉네임·카테고리를 담는다. **2026-09-19 실제 구현**: Supabase Realtime(Postgres Changes)으로 `expenses` INSERT를 구독해서 실제로 띄운다 — RLS가 그대로 적용되어 내가 볼 수 있는 행(내 것 + 내가 멤버인 그룹의 공유 지출)만 이벤트가 오고, 그중 본인이 아닌 기록만 알린다. Realtime을 받으려면 Supabase 프로젝트의 `supabase_realtime` publication에 `expenses`가 추가돼 있어야 한다(schema.sql 맨 끝).
- 카테고리 헤더 클릭(2026-09-19 팀 요청): "카테고리" 라벨을 눌러도 반응이 없었다 — 라벨 전체(3줄 버튼 포함한 줄 전체)를 눌러도 "카테고리 편집" 메뉴가 열리도록 클릭 영역을 넓혔다.
- 카테고리 항목 클릭(2026-09-19 팀 요청, 신규): 편집 모드가 아닐 때 카테고리 하나(예: "식비")를 누르면, 그 카테고리로 미리 필터링된 7(지출 목록)이 스택에 쌓여 열린다 — 7과 완전히 같은 화면을 재사용하고(`initialCategoryFilter`), 뒤로가기는 탭 전환이 아니라 2c로 돌아가는 스택 pop이다. 편집 모드에서는 원래대로 "..."로 이름만 바꾼다(클릭 동작이 겹치지 않게 편집 모드에선 항목 클릭 자체를 막는다).
- **"지출 기록 시 확인 알림"은 실제로 동작한다(2026-09-17 구현)**: 6(지출 입력)에서 새 지출을 저장하면(수정 제외) 이 토글이 켜져 있을 때 "OO원, 카테고리가 저장됐어요" 토스트가 화면 아래쪽에 잠깐 떴다가 사라진다.
- **다크모드는 실제로 동작한다(2026-09-17 팀 요청으로 구현, 같은 날 색 반전 방식 → 손으로 고른 팔레트로 교체)**: 처음엔 앱 전체를 CSS 색 반전(invert+hue-rotate)으로 어둡게 만들었으나 "색깔이 죄다 이상해진다"는 피드백을 받아, `app/globals.css`에 구조색 토큰(`--shoot-bg`·`--shoot-surface`·`--shoot-surface-alt`·`--shoot-border`·`--shoot-divider`·`--shoot-text`·`--shoot-text-muted`·`--shoot-accent`)을 정의하고 화면 전체(`components/screens/*.tsx` 등)의 페이지·카드 배경, 테두리, 본문·보조 텍스트, 브랜드 보라색 inline style을 전부 이 토큰 참조로 바꿨다. `.shoot-app-root[data-dark="true"]`에서 이 토큰 값만 다크 팔레트로 바꾸면 화면 전체가 같이 바뀐다.
  - **그대로 둔 것**: 카테고리·그룹유형 배지 색(accent/light/ink 세트), 아바타 팔레트, 브랜드 그라디언트(스플래시·홈 총지출 카드 등)와 그 위의 흰 글자, 성공/에러 등 상태색(빨강 등), 토글 스위치의 꺼짐 상태 회색 — 전부 작고 채도 높은 강조색이라 다크 배경 위에서도 배지처럼 자연스럽게 보여서 새로 칠하지 않았다.
  - **알아둘 것**: 아바타 배경(고정 파스텔)에 얹는 글자색은 테마와 무관하게 항상 고정된 어두운 색이어야 한다(`DelegateSelect.tsx`·`GroupDetail.tsx`에 주석으로 남겨둠) — 안 그러면 다크모드에서 흰 글자가 파스텔 배경 위에서 안 보인다.
- **글자 크기 select 삭제(2026-09-19 팀 요청)**: 실제 기능이 없던 select였는데, 아예 화면에서 뺐다 — "앱 외형" 카드엔 이제 다크 모드 토글만 남는다. `store.fontSize`·`FontSize` 타입도 코드에서 함께 정리했다.

### 3a. 그룹 생성 — 모임 선택
- 표시: 유형 카드 6개(가족·부부·커플·친구·모임동아리·기타)
- 형제자매 카드(2026-09-11 팀 결정): 추가하지 않는다 — 디자인 그대로 6개만 쓴다(SHY 스펙 enum엔 SIBLING이 있지만 이번 범위에선 뺀다).

### 3b-1~6. 이름 입력
- 입력: 그룹 이름(자유 텍스트), 추천 이름 칩 3개(유형별로 다름)
- 출처: R1 · SHY 스펙 Task 1-4("플레이스홀더는 선택한 유형에 따라 예시를 다르게")
- 추천 이름 칩 클릭(2026-09-18 팀 요청): 누르면 그 문구가 위 입력칸에 그대로 채워진다(이전엔 정적 표시만 했음).

### 3c. 그룹 생성 — 완료
- 표시: 완료 아이콘, 그룹명, 초대 코드(예: CP-4K9X), "초대 코드 공유하기" 버튼
- 출처: R2 · F2 · SHY 스펙 Task 1-4
- **2026-09-18 실제 구현**: 3b "그룹 만들기"가 실제 `groups`·`group_members` INSERT 2번으로 바뀌었다(`store.createGroup`).

### 4. 그룹 참여
- 입력: 초대 코드
- 예외(2026-09-11 팀 결정): "코드 없음"·"이미 참여한 그룹"(P3, 409) 모두 입력칸 밑 인라인 오류로 표시(예: "이미 참여한 그룹입니다"), 화면 전환 없음.
- **2026-09-18 실제 구현**: `groups`가 "멤버만 select 가능" RLS라 초대 코드로 아직 멤버가 아닌 그룹을 클라이언트가 직접 찾을 수 없다 — `join_group_by_invite_code` RPC(security definer, supabase/schema.sql)로 조회+가입을 한 번에 처리한다.

### 5a. 그룹 목록
- 표시: 그룹 카드(그룹명, 유형 배지, 멤버 수), "초대 코드로 참여하기" 진입 카드
- 출처: R1~R3

### 5b. 그룹 상세(피드)
- 표시: 그룹명·유형, (패널 토글 시) 멤버 목록 + 초대 코드 공유, 지출·저금 피드(작성자 아바타, 가맹점명, 카테고리, 시간, 금액 — 수입/지출 색상 구분)
- 출처: R13 · F13
- 저금 표시 방식(2026-09-11 팀 결정): 지출(Expense)과 저금(Saving)을 구역으로 나누지 않고, 시간순으로 하나의 목록에 섞어서 보여준다.
- **이번 달 그룹 예산 카드(2026-09-21 사용자 요청, 신규)**: 그룹장이 정한 예산이 있으면 진행률(지출/예산)을 보여주고, 없으면 그룹장에게만 "설정하기"가 보인다. 탭하면 groupGoalSetting으로 이동(그룹장 아니면 읽기 전용).
- **그룹원별 이번 달 지출 비교(2026-09-21 팀원 요청, 신규)**: F15(정산) 발산 질문에서 검토했던 "자동 계산 없이 비교 표만 보여주는" 절충안이 실제로 구현됐다 — 공유 지출만 집계해 그룹원별 막대로 나란히 보여준다(더치페이 자동 계산은 여전히 안 함).
- **멤버 목록 그룹장 배지(2026-09-24 사용자 요청, 신규)**: 멤버 목록에서 그룹장인 사람 이름 옆에 "그룹장" 배지가 표시된다(처음엔 "방장"으로 넣었다가 MyGroupsManage.tsx 등 나머지 화면과 용어를 맞춰 "그룹장"으로 정정).

### 6. 지출 입력
- 표시: 상단 금액 — **직접 타이핑(숫자패드/텍스트 입력) — 2026-09-11 팀 결정**, 카테고리(그룹 유형별 프리셋, 06-data.md E8 참고 + "직접 입력"), 메모, 날짜, "어느 그룹과 공유할까요?" select
- 출처: R5~R7, R12, R16 · F5~F7, F12, F16
- 진입 경로(2026-09-11 팀 결정): 7(지출 목록) 우상단에 새로 만든 "+" 버튼. 7의 항목을 탭하면 이 화면을 수정 모드로 재사용.
- **[제안] 이 화면은 그룹을 먼저 고르면(select) 카테고리 프리셋이 바뀌는 순서다.** SHY 스펙 Task 2-4도 "그룹 선택 → 프리셋 전환"으로 정해서 이 디자인과 일치함 — 근거: SHY 스펙 Task 2-4 설명
- 개인·그룹 카테고리 차별화(2026-09-17 팀 결정): "어느 그룹과 공유할까요?"에서 그룹을 안 고르면 개인 카테고리, 고르면 그 그룹만의 카테고리 목록이 칩으로 뜬다 — 그룹마다 서로 다른 카테고리를 갖는다. "직접 입력"으로 새 카테고리를 쓰고 저장하면 그 scope(개인 또는 고른 그룹)에 저장되어 2c(설정)에도 나타나고, 다음에 같은 scope에서 칩으로 바로 고를 수 있다.
- 날짜(2026-09-17 팀 결정): 고정 텍스트가 아니라 네이티브 날짜 입력(`<input type="date">`)으로 직접 선택·변경할 수 있다. 신규 입력은 오늘 날짜로 시작하고, 수정 모드는 기존 지출의 날짜로 시작한다.
- **2026-09-18 실제 구현**: "저장하기"가 실제 `expenses` INSERT/UPDATE로 바뀌었다(`store.addExpense`/`store.updateExpense`) — 카테고리(개인/그룹)·수입은 아직 목업 상태 그대로다.
- **2026-09-19 버그 수정**: 날짜 기본값(`TODAY_DATE`, lib/mock.ts)이 데모용으로 "2026-09-09"에 고정돼 있어서, 실제로 오늘 지출을 입력해도 저장된 날짜가 항상 그 고정일이 됐다(7의 기간 필터에 오늘 날짜를 걸면 안 보이는 원인). 실제 오늘 날짜(KST)로 계산하도록 고쳤다 — 2b(홈) 캘린더·이번 달 총계도 같은 값을 쓴다.
- **"매달 반복 등록"(2026-09-21 사용자 요청, 신규)**: 신규 입력(수정 모드 제외)에만 보이는 토글 — 켜고 저장하면 지금 입력한 금액·카테고리·공유 대상 그대로 정기 지출 템플릿도 함께 만들어진다(day_of_month는 고른 날짜의 일(day)). 관리는 2c "정기 지출 관리"(recurringExpenseManage)에서.
- **영수증 원본 다시 보기(2026-09-21 사용자 요청, 신규)**: 수정 모드에서 그 지출이 OCR 촬영 지출(source_type=RECEIPT)이고 원본 이미지가 저장돼 있으면, 상단에 미리보기가 뜨고 탭하면 전체화면으로 확대된다(재확인만, 여기서 사진을 바꿀 순 없음). 직접 입력한 지출에는 이 미리보기가 없다.

### 7 / 7b. 지출 목록
- 표시: 월별 그룹핑된 지출 카드(가맹점명, "확인 필요" 배지, 카테고리·그룹·시간, 금액), 데이터 없을 때(7b) 빈 상태 안내, **우상단 새 "+" 버튼(6으로) · 카테고리·기간 필터 UI(신규, 2026-09-11 팀 결정)**
- 출처: R6, R10, R13, R14 · F6, F10, F14
- 7b 빈 상태 "직접 입력하기"(2026-09-18 팀 요청): "영수증으로 기록하기" 버튼만 있던 빈 상태에도 6(지출 입력)으로 바로 들어가는 버튼을 추가했다 — 7의 "+"와 같은 목적지.
- 삭제(2026-09-19 팀 요청): 각 카드에 삭제 버튼 추가(행 클릭=수정 모드 진입과 안 겹치게 별도 버튼) — 확인 팝업 한 번 띄운 뒤 실제 `expenses` DELETE(`store.deleteExpense`, RLS expenses_delete_own_only가 본인 지출만 허용).
- **삭제 실행취소(2026-09-21 팀원 요청, 신규)**: 삭제를 확인하면 곧바로 지우지 않고 5초짜리 "되돌리기" 토스트를 띄운다 — 그 안에 누르면 삭제를 취소하고, 5초가 지나거나(또는 화면을 벗어나면 그 자리에서 확정) 그대로 두면 실제로 삭제된다.
- **정렬 옵션(2026-09-21 팀원 요청, 신규)**: 카테고리·기간 필터 옆에 정렬 select 추가(최신순 · 금액 큰순 · 금액 작은순, 기본은 최신순) — 월별 묶음 순서는 그대로 두고 묶음 안의 순서만 바뀐다.
- **영수증 썸네일(2026-09-21 사용자 요청, 신규)**: OCR 촬영 지출(source_type=RECEIPT)이고 원본 이미지가 있으면 카드 오른쪽에 작은 썸네일이 붙는다 — 탭하면(행 클릭과 안 겹치게) 원본이 전체화면으로 확대된다.

### 8a / 8b. 영수증 촬영·인식 중
- 출처: R8, R10, R11 · F8, F11 · SHY 스펙 Task 3-3
- 인식 완료 시 하단에 "인식 완료! ₩12,500 · 식비 · 스타벅스" 카드가 뜸(토스트 형태) — F8 결과("OO원, XX마트 지출이 자동으로 등록됐어요" 토스트)와 문구는 다르지만 취지는 일치
- **영수증 원본 이미지 실제 저장(2026-09-21 사용자 요청)**: 예전엔 `image_url`이 항상 null로 저장돼 나중에 원본을 다시 볼 방법이 없었다 — 이제 촬영한 사진을 캔버스로 축소·압축(최대 1280px, JPEG 72%)한 뒤 data URL로 `image_url`에 저장한다(Storage 버킷 없이 프로필 사진과 같은 방식). 압축이 실패해도 P7 원칙(인식 실패해도 저장은 항상 성공)은 그대로 유지되고, 실패 시에는 토스트로 알려준다(image_url만 null로 남음).

### 10 / 10a / 10a-1 / 10b. 마이페이지 · 내 그룹 관리 · 위임 대상 선택 · 내 정보 변경
- 표시(10): 프로필(이름·이메일 — 둘 다 1·2에서 입력한 값이 그대로 반영됨, 2026-09-14 팀 결정. 이메일은 06-data.md E1에 따라 profiles가 아니라 별도 세션 값으로 관리), "내 그룹 관리", 로그아웃
- 표시(10b, 신규 — 2026-09-17 팀 요청, "개인정보변경"까지 포함해 한 화면으로 확장): 10의 프로필 카드를 누르면 열린다.
  - 프로필 사진(원형, 우하단 카메라 버튼으로 파일 선택 — 06-data.md E1엔 없는 항목이라 store.currentUserAvatarUrl에 브라우저에서 읽은 data URL만 들고 있고, 실제로 어디 업로드하진 않는다)
  - 닉네임 입력칸(= profiles.name, 1(회원가입)에서 정한 이름과 같은 필드를 여기서 다시 바꾸는 것 — 별도의 "닉네임" 컬럼을 새로 만들지 않았다)
  - **계정 정보(2026-09-17 팀 요청)**: 이메일(1의 형식 검증과 동일하게 인라인 오류) · 비밀번호(비워두면 기존 값 유지, 채우면 그 값으로 교체) — 이메일·비밀번호 둘 다 06-data.md E1에 profiles 컬럼으로 없어(auth.users가 담당) `supabase.auth.updateUser`로 바꾼다. **2026-09-18 실제 구현**: 닉네임은 `profiles.name` UPDATE(schema.sql에 `profiles_update_own_only` 정책 추가), 이메일·비밀번호는 실제 Supabase Auth API 호출 — 이메일 변경은 프로젝트 설정에 따라 확인 메일을 거쳐야 실제로 반영될 수 있다.
  - "저장하기" → 전역 토스트("내 정보가 저장됐어요") 후 10으로 복귀.
- 표시(10a): 소속 그룹별 카드 — 그룹장인 그룹은 "그룹장 위임"·"그룹 나가기" 버튼, 일반 멤버인 그룹은 "그룹 나가기" 버튼만
- 표시(10a-1, 신규 — 2026-09-11 팀 결정): 그 그룹의 그룹원 목록을 보여주고 한 명을 고르면 위임이 실행되고 10a로 돌아간다.
- 출처: R18 · F18 · 05-policy.md P10, 상태값1
- "그룹장 위임" 다음 화면(2026-09-11 팀 결정): 10a-1(위임 대상 선택, 신규 화면).
- 10a-1 본인 표시(2026-09-17 팀 결정): 목록에서 본인을 빼지 않고 그대로 보여주되 "본인"이라고 표시하고, 본인 행은 고를 수 없게(흐리게, 클릭 안 됨) 한다 — 이전엔 본인을 목록에서 아예 뺐었다.
- "그룹 나가기" 확인 절차(2026-09-11 팀 결정): 바로 나가지 않고 확인 팝업(모달)을 한 번 띄운다 — 그룹원이 혼자뿐이면 그룹·지출이 함께 삭제되는(05-policy.md 상태값1) 되돌릴 수 없는 동작이기 때문.
- **2026-09-18 실제 구현**: "그룹장 위임"·"그룹 나가기"가 실제 `group_members`/`groups`/`expenses`/`savings` UPDATE·DELETE로 바뀌었다(`store.delegateOwner`/`store.leaveGroup`). 그룹원이 혼자뿐이면 지출·저금(둘 다 이 그룹을 참조하는 행)을 먼저 지운 다음 그룹을 지운다 — `savings.group_id` FK가 `ON DELETE RESTRICT`라 저금이 남아있으면 그룹 삭제 자체가 막히기 때문(schema.sql `savings_delete_own_only` 정책도 이때 추가됨).
- **회원 탈퇴(2026-09-18 팀원 요청, 신규)**: 10(마이페이지)에 "회원 탈퇴" 버튼 → 확인 모달 → `app/api/account`(서버, Service Role Key)를 거쳐 `auth.users`를 실제로 지운다. 011 마이그레이션으로 profiles·그 아래 데이터(그룹 멤버십·지출·저금·수입 등)가 cascade로 함께 지워진다. 카카오로 로그인한 계정이면 탈퇴 전에 카카오 쪽 연결도 끊는다(끊는 데 실패해도 탈퇴 자체는 진행 — 부가 기능이 본 기능을 막으면 안 되므로).

## 하단 탭 내비게이션
5개 탭(홈 · 그룹 · 중앙 강조 아이콘 · 지출내역 · 마이페이지)이 2b·5a·5b·6·7·7b·10 화면에 공통으로 나타난다.
- 홈 → 2b, 그룹 → 5a, 지출내역 → 7, 마이페이지 → 10
- 중앙 강조 아이콘(2026-09-11 팀 결정): 8a(영수증 촬영)로 연결한다. 6(지출 입력)은 이 아이콘이 아니라 7의 새 "+" 버튼으로 들어간다(위 참고).

## 화면 전환 방식
> 2026-09-11 팀 결정: 화면마다 라우트를 만들지 않는다. `app/page.tsx` 한 페이지가 화면들을 상태로 들고 전환한다. 위 「화면 목록」 표의 진입 경로·다음 화면(액션)은 그대로다 — 이동이 일어나는 방식만 페이지 이동에서 화면 전환(애니메이션)으로 바뀐다.
> 한 페이지로 합친 이유: 라우트를 화면마다 두면 페이지 이동마다 이전 화면이 언마운트되는데, 아래 "화면 쌓기"는 여러 겹이 동시에 마운트된 채 하나씩 벗겨지는 세로 슬라이드를 요구하고 "탭 전환"은 120ms 크로스페이드를 요구한다 — 둘 다 이전 화면이 전환 도중 화면에 남아 있어야 해서, 라우트 이동이 아니라 한 페이지 안의 상태 전환으로 구현한다.

이동은 세 종류이고 애니메이션이 다르다(로그인 전 화면 전환은 2026-09-11에 세 번째 종류로 확정됨).

| 종류 | 대상 | 애니메이션 | 뒤로가기 |
|---|---|---|---|
| (1) 탭 전환 | 「하단 탭 내비게이션」의 홈(2b)·그룹(5a)·지출내역(7)·마이페이지(10) 사이 | 슬라이드 없음, 120ms 크로스페이드 | 개념 없음(탭끼리는 뒤로가기가 없음) |
| (2) 화면 쌓기 | 탭 위에 얹히는 화면 | 세로 슬라이드 280ms · cubic-bezier(0.32, 0.72, 0, 1) — 앞으로 가면 아래에서 올라오고, 뒤로 가면 아래로 내려간다. 여러 겹 쌓이고 하나씩 벗겨진다. 쌓인 화면에는 하단 탭이 보이지 않는다 | 한 겹씩 벗겨짐 |
| (3) 로그인 전 화면 전환(신규, 2026-09-11 팀 결정) | 0·1·1b·2·2a 사이(아직 탭 셸이 없음) | 애니메이션 없이 즉시 전환 | 별도 스택 관리 없음(일반 브라우저 동작) |

**뒤로가기 버튼(UI) — 2026-09-14 팀 결정**: (2) 화면 쌓기에 속하는 화면 중 **3a·3b-1~6·3c·4·5b·6**엔 화면 왼쪽 위에 뒤로가기(‹) 버튼을 눌러 볼 수 있게 추가했다(이전엔 스택 pop 자체는 되지만 화면에 누를 수 있는 버튼이 없었다 — 예: 4·6은 제출 성공 시에만 자동으로 뒤로 갔음). 버튼은 `nav.back()`을 호출해 한 겹만 벗긴다. 3c(그룹 생성 완료)의 뒤로가기는 그룹 생성 자체를 취소하지 않는다 — 그냥 3b(이름 입력)로 돌아갈 뿐이다.

**탭 루트 화면의 뒤로가기 버튼 — 2026-09-14 팀 결정**: **5a·7·7b**(탭 루트라 원래 "뒤로가기 개념 없음")에도 왼쪽 위에 뒤로가기(‹) 버튼을 추가했다 — 단, `nav.back()`이 아니라 **`nav.switchTab("home")`으로 항상 홈(2b) 탭으로 전환**한다(탭 루트엔 쌓인 이전 화면이 없어 `nav.back()`을 쓰면 브라우저 히스토리가 앱 밖으로 빠질 수 있어서 다르게 처리함).

### 화면ID별 분류
| 화면ID | 방식 | 근거 |
|---|---|---|
| 0 | (3) 즉시 전환 | 2026-09-11 팀 결정 |
| 1 | (3) 즉시 전환 | 2026-09-11 팀 결정 |
| 1b | (3) 즉시 전환 | 2026-09-11 팀 결정 |
| 2 | (3) 즉시 전환 | 2026-09-11 팀 결정 |
| 2a | (3) 즉시 전환 | 2026-09-11 팀 결정 |
| 2b | (1) 탭 전환 | 팀 결정에 명시 |
| 2b-1a | (2) 화면 쌓기 | 화면 목록의 진입 경로("2b 수입 카드 탭")가 홈 탭(2b) 위에 얹히는 형태와 일치. 화면 자체를 만들지 여부는 2026-09-11 팀 결정으로 확정(포함) |
| 2b-1 | (2) 화면 쌓기 | 진입 경로("2b-1a '+' 버튼")가 2b-1a 위에 다시 얹히는 형태 — "여러 겹 쌓이고 하나씩 벗겨진다"는 정의와 일치 |
| 2c | (2) 화면 쌓기 | 진입 경로("10 톱니바퀴 아이콘")가 마이페이지 탭(10) 위에 얹히는 형태와 일치. 화면 자체를 만들지 여부는 2026-09-11 팀 결정으로 확정(포함) |
| 3a | (2) 화면 쌓기 | 팀 결정에 명시 |
| 3b-1~6 | (2) 화면 쌓기 | 팀 결정에 명시 |
| 3c | (2) 화면 쌓기 | 팀 결정에 명시 |
| 4 | (2) 화면 쌓기 | 팀 결정에 명시 |
| 5a | (1) 탭 전환 | 팀 결정에 명시 |
| 5b | (2) 화면 쌓기 | 팀 결정에 명시 |
| 6 | (2) 화면 쌓기 | 팀 결정에 명시 |
| 7 | (1) 탭 전환 | 팀 결정에 명시 |
| 7b | 해당 없음(전환 아님) | 화면 목록에 "지출이 하나도 없을 때 7 대신 표시"라고 돼 있어, 7과 같은 탭 자리의 데이터 없음 상태일 뿐 별도 전환이 일어나지 않음 |
| 8a | (2) 화면 쌓기 | 진입 경로(하단 탭 카메라 아이콘 · 7b 버튼)가 지출내역 탭(7) 위에 얹히는 형태와 일치 — 2026-09-11 팀 결정으로 진입 경로 확정 |
| 8b | (2) 화면 쌓기 | 8a 위에 이어서 얹히는 형태 |
| 10 | (1) 탭 전환 | 팀 결정에 명시 |
| 10a | (2) 화면 쌓기 | 진입 경로("10 '내 그룹 관리'")가 마이페이지 탭(10) 위에 얹히는 형태와 일치 |
| 10a-1 | (2) 화면 쌓기 | 10a 위에 이어서 얹히는 형태(신규, 2026-09-11 팀 결정) |
| 10b | (2) 화면 쌓기 | 10 위에 얹히는 형태(신규, 2026-09-17 팀 요청) |

## 저금통 펫 키우기 v2 (docs/08-pet-feature-spec.md + hybranch F22 + shooTbranch 목표 + 참고 이미지 통합, 2026-09-15)
> 01~05 문서 어디에도 근거가 없는 신규 제안이라 R/F 번호는 없다(단, F22·F23은 사용자가 이번 요청에서 번호를 붙여 지칭해 그대로 씀). **2026-09-15: hybranch·shooTbranch·mg 세 브랜치를 사용자 요청으로 통합** — 참고 이미지("캐릭터 커스텀" 화면, 5b-1)의 색상 커스텀 UI, hybranch의 다마고치형 그룹 공통 반려 캐릭터(F22)와 방치 시 시무룩 상태, shooTbranch의 목표(퀘스트) 시스템, hybranch의 그룹 피드 이모지 반응(F23)을 이번 v1 구현 위에 얹었다. 충돌 지점 4곳은 AskUserQuestion으로 사용자에게 직접 확인(아래 "충돌 해소" 참고), 그 외 세부 알고리즘(그룹 펫 XP 산정 등)은 여전히 [?]로 남겨 스펙을 지어내지 않았다.

| 화면ID | 화면명 | 진입 경로 | 다음 화면(액션) |
|---|---|---|---|
| P1 | 펫 선택(PetSelect) | 마이페이지 "저금통 코인 카드"(개인 펫 없으면) 탭 · 5b(그룹 상세) 그룹 펫 카드(그룹 펫 없으면) 탭 — 수동 진입(1번 [?] 참고) | v2: 종 선택 없이 기본 색상 마스코트 미리보기 + 이름 입력(선택) → "펫 데려오기" → P2 |
| P2 | 펫 상세·성장(PetDetail) | P1 완료 후, 마이페이지 코인 카드·5b 그룹 펫 카드·목표 리포트(P4) "펫 보러가기"에서 재진입. scope(개인/그룹)를 받아 개인 펫·그룹 펫 둘 다 이 화면을 같이 쓴다 | 성장 트래커·XP 바 · 개인 펫: "밥 주기" 버튼 + 저금통 코인·목표 리포트·배지·**"꾸미기"(→P8)** 링크 · ~~그룹 펫: 밥 주기 버튼 없이 "그룹원이 지출을 나눠 기록하면 자란다"는 안내문만 표시(자동 성장, F22)~~ **2026-09-21 사용자 요청으로 변경**: 그룹 펫도 "밥 주기" 버튼이 생겼다 — 그룹원 각자 하루 1번씩, 참여도 기반 자동 성장(F22)은 그대로 유지한 채 수동 밥주기가 추가됐다(형평성을 위해 XP를 그룹원 수로 나눠 받음) |
| P3 | 데일리 먹이주기 팝업(PetFeedPopup) | 개인 펫만. 지출을 하나라도 새로 기록한 직후(수정 제외), 오늘 아직 안 먹였을 때만 | "밥 주기" → XP 지급 · 확인 버튼으로 닫힘(모달, 새 화면 아님) |
| P4 | 이번 달 목표 리포트(MonthlyGoalReport) | **v2로 명칭·내용 교체(구 P4 WeeklySavingsReward 폐기)** — P2(개인 펫 상세)의 "이번 달 목표" 버튼. 화면을 열 때 그 자리에서 계산(v1과 동일 패턴) | 카테고리별 달성/미달성 카드 + 달성 시 고정 보상(코인·XP) 표시 → "펫 보러가기"(P2) |
| P5 | 나의 배지(MyBadges) | 마이페이지 "나의 배지" 카드 · P2(개인 펫)의 "나의 배지" 버튼 | 화면 전환 없음(v2: 5단계→**4단계** 배지 목록만 표시) |
| P6 | 그룹 저금통 랭킹(GroupPetRanking) | **여전히 뺐다.** v2에서도 그룹 펫 XP 산정을 잠정값(아래 참고)으로만 구현했을 뿐 팀이 확정하지 않아, 순위를 매기는 화면은 만들지 않았다 | — |
| P7 | 예산 설정(BudgetSetting) | **v2에서 폐기.** shooTbranch 통합으로 "예산" 개념 자체가 "목표"로 대체됐다(사용자 확인: "목표 설정으로 대체") | — (아래 P7' 참고) |
| P7' | 월별 목표 설정(MonthlyGoalSetting) | **v2 신규, P7 자리를 대체.** 2c(설정) "저금통 펫" 섹션 "월별 목표 설정" 행 | 카테고리별 이번 달 목표 금액 입력(최근 3개월 평균 참고 표시) → "저장하기" → 2c로 복귀 |
| P8 | 캐릭터 커스텀(PetCustomize) | **v2 신규(참고 이미지 그대로).** P2(개인 펫 상세)의 "꾸미기" 링크 — 개인 펫 전용, 그룹 펫엔 없음(사용자 확인: "마스코트 하나로 통일") | 몸/가계부/가방/눈/잎사귀 5개 부위 색상 팔레트 선택 · **2026-09-21 신규**: 성장 단계 미리보기도 고를 수 있고(이미 도달한 단계까지만) 이제 `display_stage_index`로 실제 저장됨(전엔 "적용하기"를 눌러도 저장이 안 돼 다른 화면엔 늘 실제 성장 단계만 보였음) → "적용하기" → P2로 복귀 |
| P9 | 개인 랭킹(PersonalRanking) | **2026-09-17 사용자 요청, 신규** — 마이페이지 "개인 랭킹" 카드 | 개인 펫끼리(그룹 무관)만 성장 지표로 순위 비교(화면 전환 없음). P6(그룹 랭킹)과 달리 그룹 펫 XP 산정 미정과 무관하게 구현 가능해 먼저 만들어졌다 |

- 마이페이지(10) 변경: "저금통 코인 카드"(펫 없으면 "펫 만들러 가기", 아이콘은 v2부터 종이 아니라 마스코트+개인 색상)·"나의 배지 카드"(N/4) 2개 — 출처: docs/08-pet-feature-spec.md §7
- 5b(그룹 상세) 변경: 그룹 펫 카드(펫 없으면 만들기, 있으면 P2로, 마스코트 표시) + **F23: 공유 지출 피드 카드마다 이모지 반응 UI**(집계된 이모지+개수 배지, "+ 반응" 버튼으로 팔레트 열어 토글 추가/삭제) — §6의 미니 랭킹 카드는 P6과 함께 계속 뺌
- 2c(설정) 변경: "저금통 펫" 섹션의 행을 "주간 예산 설정"(P7)에서 **"월별 목표 설정"(P7')**으로 교체 — 출처: shooTbranch 통합
- 홈(2b) 변경(**2026-09-20 신규, 사용자 요청**): 맨 위에 "출석체크" 카드 추가 — 접속률을 올리려는 목적이라 새 화면(P번호)이 아니라 홈을 열면 바로 보이는 자리에 얹었다. 하루 1번 탭하면 코인 지급, 7일 연속 출석하면 7일째 코인 2배(`lib/store.tsx` `checkInToday`, `lib/pets.ts` `CHECKIN_*`)

### 충돌 해소(2026-09-15, AskUserQuestion으로 사용자에게 직접 확인)
1. **개인 펫 vs 그룹 펫을 하나의 반려 캐릭터 개념으로 합칠지, 완전히 분리할지** — 사용자 선택: "하나로 합침(추천)". 결과: v1처럼 `pets` 테이블 한 행 구조를 유지하되, 그룹 펫의 의미를 hybranch의 F22(그룹 공통 반려 캐릭터)로 재정의했다.
2. **성장 단계 수(v1의 5단계 vs 참고 이미지의 4단계)** — 사용자 선택: "이미지대로 4단계(추천)". 결과: "전설" 단계 폐기, 알→유년기→청소년기→성체 4단계로 통일. "시무룩"은 5번째 단계가 아니라 청소년기의 시각 변형(파생 상태)으로 처리.
3. **마스코트를 종(v1의 백호/강아지/고양이/흑룡)별로 다르게 할지, 이미지처럼 하나의 마스코트+색상 커스텀으로 통일할지** — 사용자 선택: "마스코트 하나로 통일(추천)". 결과: `species` 컬럼 폐기, 5개 부위 색상 컬럼 신규 추가.
4. **shooTbranch의 목표(예산) 시스템을 v1의 예산·주간정산과 어떻게 합칠지** — 사용자 선택: "목표 설정으로 대체". 결과: E11 Budget·E10 WeeklySettlement 폐기, E12 CategoryGoal·E13 GoalReward 신규(월별·카테고리별, 퀘스트 클리어형 고정 보상).

### 구현 전 확인 필요 — [?] 목록과 실제 구현에서 임시로 정한 것(v1에서 이어지는 것 + v2 신규)
1. **P1 진입 시점**: 스펙엔 "최초 1회"라고만 있고 정확한 시점이 없었다 — 자동 트리거 없이 수동 진입으로 정했다(가장 단순하고 되돌리기 쉬운 선택). 팀이 "회원가입 직후 자동으로 띄우자"고 정하면 바꿔야 함
2. **그룹 펫(F22) XP 산정 알고리즘**: hybranch 스펙("그룹원 참여도에 따라 성장")이 구체적 알고리즘까지 정하진 않아서, "최근 7일 내 공유 지출을 기록한 그룹원이 2명 이상이면 정상 XP, 1명뿐이면 절반"으로 잠정 구현했다(`lib/store.tsx` `growGroupPetFromSharedExpense`, `lib/pets.ts`). 정확한 창 길이·경계 인원수는 팀이 정할 [?] — **P6(그룹 랭킹)을 계속 빼둔 이유이기도 하다**
3. **"시무룩(sulking)" 판정 기준일**: 개인 펫 2일·그룹 펫 3일 방치 시(`lib/pets.ts` `PERSONAL_SULK_AFTER_DAYS`/`GROUP_SULK_AFTER_DAYS`)로 임의 설정 — 스펙·이미지 어디에도 정확한 일수가 없어 잠정값. 방치해도 성장 단계가 내려가진 않는다(시각 변화만)
4. **목표(퀘스트) 달성 보상 수치**: 고정 코인 10·XP 30(`lib/pets.ts` `GOAL_ACHIEVED_REWARD_COINS`/`GOAL_ACHIEVED_REWARD_XP`)으로 임의 설정 — "먹이를 더 주는 형식이나 경험치를 더 주는 형식"이라는 사용자 요청을 "달성 시 고정 보상 지급"으로 해석했다(절약 비율 비례가 아님). 정확한 값은 팀이 정할 [?]
5. **F23 반응 가능 이모지 목록**: 문서 근거가 없어 임의로 24종 팔레트를 만들었다(`lib/pets.ts` `REACTION_EMOJI_PALETTE`) — 정확한 목록·개수 제한은 팀이 정할 [?]
6. **밥주기 1회당 XP 기본값**: 여전히 15(v1과 동일, 스펙 §3 "기본값 15") — 팀이 정해야 확정값
7. **하단 탭·내비게이션에 펫 진입점**: 전용 탭 없이 마이페이지·그룹 상세 카드로만 진입(1번과 같은 이유) — 팀이 전용 탭·아이콘을 원하면 추가해야 함
8. **"예산 초과 시 알림" 삭제 결정(2c, 2026-09-17)**: v2에서 예산 개념 자체가 목표로 대체되면서 자연히 더 이상 되살릴 필요가 없어졌다고 보고 손대지 않았다 — 목표 초과 알림이 필요한지는 별도로 팀이 정할 [?]
9. **출석체크 보상 수치(2026-09-20 신규)**: 하루 기본 코인 5·7일 연속 시 2배(`lib/pets.ts` `CHECKIN_REWARD_COINS`/`CHECKIN_STREAK_BONUS_MULTIPLIER`)로 임의 설정 — "코인 얻는다", "일주일 꼬박 출석해서 코인 두배씩"이라는 사용자 요청은 지급 방식(주기 완주 시 그날 코인 2배, 다음 날 1일째로 리셋)만 정했을 뿐 정확한 숫자는 없어 GOAL_ACHIEVED_REWARD_COINS와 같은 성격의 placeholder다. XP는 지급하지 않는다(사용자가 코인만 언급함) — XP도 줄지는 팀이 정할 [?]
10. ~~**펫 경제 개편이 필요한지**~~ **2026-09-21 사용자가 수치까지 직접 요청해 해결**: 밥값 1→5코인 인상, 단계별 필요 XP 등차 증가(`[100,150,200]`), 지출 기록 시 코인 지급(개인·그룹 각 3코인, 하루 첫 기록만), 그룹 밥주기 추가(2번 항목의 "그룹원 참여도 XP" 자동 성장은 그대로 두고, 수동 밥주기를 별도로 더함) — 이 수치들은 6번·4번 항목(밥주기 XP=15, 목표 보상 코인=10·XP=30)과 달리 **에이전트가 임의로 정한 placeholder가 아니라 사용자가 직접 지정한 값**이라 더 이상 [?]가 아니다. (반면 6번·4번은 여전히 임의값 [?]로 남아 있음 — 이번 개편에서 바뀐 건 밥값·단계별 XP·지출 코인뿐이다.) **함께 고친 버그**: 목표 달성 보상·출석체크 코인이 "펫을 아직 안 만든 상태"에서 지급되면 영영 사라지던 문제 — `createPet`이 그동안 쌓인 코인·XP를 백필해서 시작값으로 넣어주도록 수정. **동시성**: 그룹원 여럿이 거의 동시에 지출·밥주기를 하면 코인·XP 갱신이 유실되던 버그 3건을 `feed_pet()`/`grow_group_pet()` 원자적 RPC로 수정(06-data.md E18, 05-policy.md P15 참고) — 2번 항목의 XP 산정 알고리즘 자체(창 길이·경계 인원수)는 이번 개편과 무관하게 여전히 미확정 [?].

## 문서(01~05)에는 있으나 디자인엔 없는 화면·기능
| 후보 | 근거(문서) | 비고 |
|---|---|---|
| 연락처로 초대(F4) | 04-features.md F4 | 그룹 초대 화면(3c)엔 초대 코드 공유만 있고 연락처 선택 UI 없음 — 04에도 이미 "우선순위 하·v2"로 분류돼 있어 이번 범위엔 없을 수 있음(이번 인터뷰에서 다루지 않음) |
| 간편결제 캡쳐 등록(F9) | 04-features.md F9 · SHY 스펙 Task 4-1·4-2 | 영수증 촬영(8a·8b)만 있고 카카오페이·토스·네이버페이 캡쳐 업로드 화면 없음 — 04에도 "우선순위 하·v2"(이번 인터뷰에서 다루지 않음) |
| 정산(더치페이) 화면(F15) | 04-features.md F15 · 03-requirements.md R15 | "정산하기" 버튼·계산 결과 화면이 디자인 전체에 없음(화면 번호 "9."가 아예 없음, 이번 인터뷰에서 다루지 않음) |
| 월별 리포트 화면(F17) | 04-features.md F17 · SHY 스펙 Task 6-1·6-2 | 도넛차트·카테고리 비중 화면 없음(홈의 막대 비중 요약과는 별개 — 위 "2b" 상세 참고, 이번 인터뷰에서 다루지 않음) |
| 저금 추가 입력 화면(F19·F20) | 04-features.md F19·F20 · 03-requirements.md R19·R20 | 06-data.md E6 참고 — 그룹 피드(5b)에서 시간순으로 함께 보여주기로는 정했지만(2026-09-11), 저금을 직접 "추가"하는 입력 폼 자체는 이번 인터뷰에서 다루지 않아 여전히 없음 |
| 지출 수정 폼(F14) | 04-features.md F14 | **2026-09-11 해결**: 7(지출 목록)에서 항목 탭 시 6(지출 입력)을 수정 모드로 재사용하기로 팀이 정했다 |

## 검토했으나 제외
| 후보 | 출처 | 제외 이유 |
|---|---|---|
| 수입(Income) 화면(2b-1·2b-1a)을 이 문서의 정식 화면 목록에서 빼는 것 | 종합 시 검토 | 디자인에 실제로 존재하는 화면이라 빼지 않고 목록엔 넣어 뒀고, 2026-09-11 팀 결정으로 실제로 이번 범위에 포함됐다 |
